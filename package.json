{
  "name": "pharmalix-ai-medic",
  "version": "1.0.0",
  "description": "An AI-powered platform for instant medical triage, connecting users with doctors, analyzing lab reports, and facilitating donations for medical aid.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
