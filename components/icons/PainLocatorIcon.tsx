import React from 'react';
import { MapPin } from 'lucide-react';

const PainLocatorIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
  <MapPin {...props} />
);

export default PainLocatorIcon;
