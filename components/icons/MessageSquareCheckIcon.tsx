import React from 'react';
import { ClipboardCheck } from 'lucide-react';

const MessageSquareCheckIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
  <ClipboardCheck {...props} />
);

export default MessageSquareCheckIcon;