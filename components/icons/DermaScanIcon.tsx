
import React from 'react';
import { Layers } from 'lucide-react';

const DermaScanIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
  <Layers {...props} />
);

export default DermaScanIcon;
