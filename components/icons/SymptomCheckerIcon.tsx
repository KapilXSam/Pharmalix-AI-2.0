import React from 'react';
import { HeartPulse } from 'lucide-react';

const SymptomCheckerIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
  <HeartPulse {...props} />
);

export default SymptomCheckerIcon;
